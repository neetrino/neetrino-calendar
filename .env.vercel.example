# ============================================
# Переменные окружения для Vercel
# ============================================
# Скопируйте в Vercel: Settings → Environment Variables.
# Добавляйте по одной переменной (Name / Value).
# VERCEL, VERCEL_ENV, NODE_ENV задаются автоматически — не добавляйте их.
# ============================================

# --- Обязательно для работы логина и API ---
# Секрет для сессий (минимум 32 символа). Сгенерировать: openssl rand -base64 32
AUTH_SECRET=your-secret-key-here-minimum-32-characters-long

# --- База данных (обязательно) ---
# PostgreSQL, например Neon:
# DATABASE_URL=postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require
DATABASE_URL=

# --- Опционально (по умолчанию включено) ---
# Включить лимиты запросов: true или false
# RATE_LIMIT_ENABLED=true

# Включить защиту CSRF: true или false
# ENABLE_CSRF_PROTECTION=true
